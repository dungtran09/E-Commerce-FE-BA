import React from "react";
import { NavBar, Sidebar } from "..";
import icons from "../../../utils/icons";

const Header = () => {
  return (
    <header>
      <NavBar />
    </header>
  );
};

export default Header;
